/**
 * react-block-observer
 *
 * A lightweight React library for efficiently loading content in lists by
 * tracking blocks of items and observing when unloaded blocks come into view.
 *
 * @packageDocumentation
 */

export { BlockObserver } from './BlockObserver';

export type { BlockObserverProps, OnIntersection } from './BlockObserver';
export type { IntersectionParams } from './types';
